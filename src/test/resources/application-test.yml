spring:
  data:
    mongodb:
      auto-index-creation: true
  # The MongoDB URI will be set by the TestContainer

# Disable context path for testing
server:
  port: 0
  servlet:
    context-path: /api 